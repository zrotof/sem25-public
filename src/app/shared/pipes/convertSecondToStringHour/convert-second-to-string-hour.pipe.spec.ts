import { ConvertSecondToStringHourPipe } from './convert-second-to-string-hour.pipe';

describe('ConvertSecondToStringHourPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSecondToStringHourPipe();
    expect(pipe).toBeTruthy();
  });
});
