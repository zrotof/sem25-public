import { HeroSwiperDirective } from './hero-swiper.directive';

describe('HeroSwiperDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroSwiperDirective();
    expect(directive).toBeTruthy();
  });
});
